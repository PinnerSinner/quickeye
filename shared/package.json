{
  "name": "@quickeye/shared",
  "version": "0.1.0",
  "description": "Shared game logic: deck generation, match validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "bad-words": "^4.0.0"
  }
}
