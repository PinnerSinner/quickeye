version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm -w shared run build
    build:
      commands:
        - npm -w client run build
  artifacts:
    baseDirectory: client/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
