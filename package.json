{
  "name": "quickeye",
  "version": "0.1.0",
  "description": "Real-time multiplayer card-matching game inspired by Dobble",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "workspaces": [
    "client",
    "server",
    "shared",
    "infra"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
